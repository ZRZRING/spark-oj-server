# TYPE  DATABASE        USER            ADDRESS                 METHOD
# Local connections (inside container)
local   all             all                                     trust
# IPv4 connections from Docker host
host    all             all             0.0.0.0/0               md5
# IPv6 connections
host    all             all             ::/0                     md5